<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
</head>
<body id='bodyWrap'>

<fieldset>
    <legend>最新 log</legend>
</fieldset>
<!--<form class="form-horizontal" method="post">-->
    <!--<h3> 總量 : <%# totalcount %>筆資料-->
        <!--<button type="submit" class="btn btn-primary">show</button>-->
    <!--</h3>-->
<!--</form>-->

<!--<p></p>-->

<% if (resp) { %>
<!--<h3>顯示結果</h3><br/>-->
<% if (resp.length == 0) { %>
<h4>查無資料!</h4>
<% } else{ %>
<h3> 總量 : <%= totalcount %>筆資料,  顯示 <%= (page - 1) * 10 +1 %>-<%= (page - 1) * 10 + resp.length %></h3>
<!--<div>-->
    <ul class="pagination">
        <% if (!isFirstPage) { %>
        <li class="prepage"><a href="?p=<%= page-1 %>"><i class="icon-chevron-left"></i>上頁 </a></li>
        <% } %>
        <% if (!isLastPage) { %>
        <li class="nextpage"><a href="?p=<%= page+1 %>"> 下頁<i class="icon-chevron-right"></i></a></li>
        <% } %>
    </ul>
<!--</div>-->
<!--<h4> 回傳最新 <%= resp.length %> 筆</h4>-->

<table class="table table-hover table-condensed">
    <caption></caption>
    <thead>
    <tr>
    <tr>
        <td> </td>
        <td>時間</td>
        <th>來源</th>
        <th>訊息ID</th>
        <th>分類</th>
        <th>嚴重</th>
        <th>識別</th>
        <th>訊息</th>
        <th>擴充</th>
        <th></th>
        <!--<th>_id</th>-->
    </tr>
    </thead>
    <thead>
    <tr>
    <tr>
        <td></td>
        <th>Timestamp</th>
        <th>device_name</th>
        <th>Identifier</th>
        <th>Facility</th>
        <th>Severity</th>
        <th>Mnemonic</th>
        <th>Message</th>
        <th>Enrich</th>
        <th></th>
        <!--<th>_id</th>-->
    </tr>
    </thead>
    <tbody>
    <% var i=1; %>
    <% resp.forEach( function(body){ %>
    <tr>
        <td><%= i++ %></td>
        <% if(body.time) {
        if(body.time.toString().length > 12){ %>
        <td><%= body.time.toISOString() %></td>
        <% } else { %>
        <td><%= body.time.toISOString() %></td>
        <% } } %>
        <td><%= body.device_name %></td>
        <td><%= body.identifier %></td>
        <td><%= body.facility %></td>
        <td><%= body.severity %></td>
        <td><%= body.mnemonic %></td>
        <td><%= body.message %></td>
        <td><%= body.enrich %></td>
        <!--<td><%# body._id %></td>-->
    </tr>
    <% }); %>
    </tbody>
</table>
<% } %>
<% } else { %>

<% } %>

</body>
</html>